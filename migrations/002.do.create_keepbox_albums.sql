CREATE TABLE keepbox_albums (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    title TEXT NOT NULL,
    date_created TIMESTAMP DEFAULT now() NOT NULL,
    user_id INTEGER
        REFERENCES keepbox_users(id) ON DELETE CASCADE NOT NULL
);